# Generated Documentation

## Project: github-copilot-jenkins-demo

Demo project showing GitHub Copilot CLI integration with Jenkins

### Installation

● Read package.json
  └ 33 lines read

```markdown
## Installation

**Prerequisites:** Node.js ≥ 18

```bash
# Install all dependencies (dev included)
npm install
```

## Available Scripts

| Command | Description |
|---------|-------------|
| `npm run build` | Compile TypeScript → `dist/` |
| `npm start` | Run compiled app |
| `npm run dev` | Run with `ts-node` (no build needed) |
| `npm test` | Run Jest tests |
| `npm run lint` | Lint TypeScript source |
| `npm run clean` | Remove `dist/` directory |
```

● Continuing autonomously (1 premium request)


Changes   +0 -0
Requests  2 Premium (17s)
Tokens    ↑ 88.6k • ↓ 497 • 76.8k (cached)

### Usage

● Read package.json
  └ 33 lines read

## Running the Project

### Prerequisites
```bash
npm install
```

### Available Commands

| Command | Description |
|---|---|
| `npm run dev` | Run directly with `ts-node` (no build needed) |
| `npm run build` | Compile TypeScript → `dist/` |
| `npm start` | Run compiled output (`dist/index.js`) |
| `npm test` | Run Jest tests |
| `npm run lint` | Lint TypeScript source files |
| `npm run clean` | Remove `dist/` directory |

### Typical Workflow

```bash
# Development (no compile step)
npm run dev

# Production
npm run build
npm start
```


Changes   +0 -0
Requests  1 Premium (13s)
Tokens    ↑ 58.6k • ↓ 381 • 47.2k (cached)
